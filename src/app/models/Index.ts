export class Index {
    index: string
    name: string
    url: string

  constructor(index: string, name: string, url: string) {
    this.index = index
    this.name = name
    this.url = url
  }

}